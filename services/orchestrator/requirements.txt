# =============================================================================
# Orchestrator — Pinned dependencies (V24.1.6 2026-06-27)
# Base image: python:3.10-slim
#
# PINNING PHILOSOPHY:
#   All direct deps pinned to ==exact to guarantee reproducible builds.
#   Versions frozen from the last successful 3-min build (6/25 commit 4dd390d).
#   Prevents pip resolver backtracking from upstream GCP version bumps
#   (e.g. google-cloud-firestore 2.28.0 tightened protobuf bounds, adding
#   ~4 min of resolver overhead per build × 8 services).
#
#   To update: run `pip install --upgrade <pkg>` locally, test, then
#   update the pin here. Never use >= in CI — it creates non-deterministic builds.
# =============================================================================

# ── Web framework (direct: Flask app) ─────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# ── Firebase / Auth (direct imports) ─────────────────────────────────────────
firebase-admin==6.5.0

# ── GCP services (direct imports) ─────────────────────────────────────────────
google-cloud-firestore==2.27.0
google-cloud-tasks==2.23.0
google-cloud-secret-manager==2.29.0
google-cloud-kms==3.14.0
google-cloud-bigquery==3.42.1
google-cloud-bigquery-storage==2.39.0
google-cloud-storage==3.12.0

# ── Vertex AI (direct imports — vertexai module is inside this package) ───────
google-cloud-aiplatform==1.158.0

# ── gRPC — pinned to binary-compatible version ────────────────────────────────
grpcio==1.81.1

# ── HTTP client (direct import) ───────────────────────────────────────────────
httpx==0.28.1

# ── Crypto (direct import — Fernet) ──────────────────────────────────────────
cryptography==49.0.0

# ── Data validation (direct import) ──────────────────────────────────────────
pydantic==2.13.4

# ── Utilities (direct imports) ────────────────────────────────────────────────
PyPDF2==3.0.1
cachetools==7.1.4
