steps:
# Enterprise Immutable Firebase Pipeline
- name: 'node:20'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    npm install -g firebase-tools
    firebase deploy --project lead-sniper-prod --only hosting,firestore --non-interactive

options:
  logging: CLOUD_LOGGING_ONLY
