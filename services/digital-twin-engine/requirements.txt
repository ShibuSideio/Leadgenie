# ── Digital Twin Engine — dependencies ──────────────────────────────────────
# Intentionally minimal; avoids Playwright/BeautifulSoup heavy deps.
# httpx is used only for the lightweight meta-fallback scrape (not full DOM).
Flask==3.0.0
gunicorn==21.2.0
httpx>=0.26.0
google-cloud-secret-manager>=2.16.2
google-cloud-aiplatform>=1.45.0
vertexai>=1.45.0
firebase-admin>=6.5.0
grpcio==1.60.0
google-api-core>=2.17.1
tenacity==8.2.3
