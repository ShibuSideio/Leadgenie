# =============================================================================
# Orchestrator — Python dependencies (V23 Enterprise Modular Architecture)
# Last updated: 2026-04-17 — V23 refactor adds flask-cors (Blueprint CORS),
#   vertexai SDK alias, and pins Werkzeug to prevent Flask 3.x incompatibility.
#
# Pinning strategy:
#   == : exact pin — security-sensitive or known-breaking minor bumps
#   >= : minimum floor — SDK-stable, allows patch/minor security fixes
# =============================================================================

# ── Web framework ──────────────────────────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
Werkzeug>=3.0.3

# ── GCP SDK ────────────────────────────────────────────────────────────────────
firebase-admin>=6.5.0
google-cloud-firestore>=2.16.0
google-cloud-tasks>=2.16.0
google-cloud-secret-manager>=2.20.0
google-cloud-bigquery>=3.20.0
google-cloud-storage>=2.16.0
google-cloud-kms>=2.21.3
google-cloud-aiplatform>=1.51.0
google-auth>=2.29.0
google-api-core>=2.19.0
grpcio>=1.63.0
protobuf>=4.25.3

# ── AI / ML ────────────────────────────────────────────────────────────────────
# vertexai is the top-level alias for google-cloud-aiplatform >= 1.38
# Both names resolve to the same package; vertexai is the canonical import.
vertexai>=1.51.0

# ── HTTP client ────────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── Utilities ──────────────────────────────────────────────────────────────────
cryptography>=42.0.0
PyPDF2>=3.0.1
cachetools>=5.3.3

# ── V23 additions ──────────────────────────────────────────────────────────────
# pydantic: used in analytics_service.validate_and_build_ue_update type guards
pydantic>=2.6.0
