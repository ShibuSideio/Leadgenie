# =============================================================================
# Orchestrator — Direct-dependency pins only (V23 final 2026-04-17)
# Base image: python:3.10-slim
#
# PINNING PHILOSOPHY (learned from 4 failed builds):
#   Only pin packages your code DIRECTLY imports. Pinning transitive deps
#   creates cross-package protobuf/grpcio conflicts that are impossible to
#   manually resolve as the GCP ecosystem updates.
#   --prefer-binary in the Dockerfile prevents source compilation.
# =============================================================================

# ── Web framework (direct: Flask app) ─────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# ── Firebase / Auth (direct imports) ─────────────────────────────────────────
firebase-admin==6.5.0

# ── GCP services (direct imports) ─────────────────────────────────────────────
google-cloud-firestore>=2.14.0
google-cloud-tasks>=2.16.3
google-cloud-secret-manager>=2.20.0
google-cloud-kms>=2.21.3
google-cloud-bigquery>=3.17.2
google-cloud-bigquery-storage>=2.25.0
google-cloud-storage>=2.14.0

# ── Vertex AI (direct imports — vertexai module is inside this package) ───────
google-cloud-aiplatform>=1.45.0

# ── gRPC — floor only, let pip find a binary-compatible version ───────────────
# No upper bound. The Dockerfile --prefer-binary flag prevents source builds.
grpcio>=1.60.0

# ── HTTP client (direct import) ───────────────────────────────────────────────
httpx>=0.26.0

# ── Crypto (direct import — Fernet) ──────────────────────────────────────────
cryptography>=38.0.0

# ── Data validation (direct import) ──────────────────────────────────────────
pydantic>=2.7.0

# ── Utilities (direct imports) ────────────────────────────────────────────────
PyPDF2==3.0.1
cachetools>=5.3.0
